%
% book.tex
%    システム制御情報学会Ａ４版クラスファイル
%    scitrans.cls のサンプル（書評のテンプレートファイル）
%
\documentclass[J,B]{scitrans}
\usepackage{graphicx}
%
%               Usage:  \documentclass[J,B]{scitrans} (和文の場合)
%                       \documentclass[E,B]{scitrans} (英文の場合)
%
% 年, 巻, 号 ページの設定
%
%
\appearyear{1995}
\vol{7}
\numberinvol{13}
\setcounter{page}{3}
\setcounter{volumepage}{10}

\Journal{書評}{}{\review}

%%\ForSubmission                %% 提出前に一度コメントをはずして
                                %% 英文題目および著者名をご確認ください

\begin{document}
\headingtitle{}
\headingauthors{}

\articleinit       % これを入れて下さい. 

\begin{Bookreview}{システム制御情報学会 システム太郎}
 \booktitle{システム制御学会のすべてシステム制御学会のすべて}%
           {システム太郎 著}%
           {制御出版}%
           {2001年4月}%
           {1頁}%
           {1,000}%
           {9-999-9999-9}

2つの書評を1頁におさめる必要がありますが, 本マクロではとりあえずスペー
スを制限する機構はつけておりません. 文章の量を適当に調節する必要があり
ます. booktitleのそれぞれの引数が長過ぎるとおかしくなるかもしれません.

システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 

システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 

システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 

\end{Bookreview}

\begin{Bookreview}{システム制御情報学会 システム太郎}
 \booktitle{システム制御学会のすべてシステム制御学会のすべて}%
           {システム太郎 編}%
           {制御出版}%
           {2001年4月}%
           {1頁}%
           {1,000}%
           {9-999-9999-9}

2つめの記事はこのように続けて下さい. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 

システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 

システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 
システム制御学会ではこのようなことを行っています. 

\end{Bookreview}



\end{document}
%
%% end of book.tex
